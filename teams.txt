git init
rm -r .git
rm-rf .fit
git status
git add teams.txt
git commit -m "Adding the teams.txt file"
git log
type Q or q if you want to get out of the reader

 two qualities to keep in mind when making a commit:
1) The size of your commit
2) The message of your commit

You can only use git diff to compare changes that haven't been staged.
    git diff
The -A flag will add all changes to the staging area, including new files
   git add -A
you should just plan to quit vi if you accidentally get into it. To do so, type :q!. Then, press Enter.

 git commit command only commits staged changes.

to pull the code from remote repository:
https://github.com/Lalitha1030/getting-started-with-git.git
echo "# getting-started-with-git" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Lalitha1030/getting-started-with-git.git
git push -u origin main

push an existing repository from the command line
git remote add origin https://github.com/Lalitha1030/getting-started-with-git.git
git branch -M main

git push to push your commits to the remote origin on the main branch. The -u flag sets the default upstream reference. This essentially means that for all future times that you want to push, you can just type git push.

git push -u origin main


 git remote -v
output:
origin  https://github.com/Lalitha1030/getting-started-with-git.git (fetch)
origin  https://github.com/Lalitha1030/getting-started-with-git.git (push)

